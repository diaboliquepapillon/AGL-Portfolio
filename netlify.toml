[build]
  publish = "."
  functions = "netlify/functions"

# Send /api/* to the serverless proxy (so "This Week" and region data work)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api?path=:splat"
  status = 200
  force = true
